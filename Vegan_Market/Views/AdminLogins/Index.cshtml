@model IEnumerable<Vegan_Market.Models.AdminLogin>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.admin_email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.admin_password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.admin_repassword)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.admin_email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.admin_password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.admin_repassword)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AdminLogin_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.AdminLogin_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AdminLogin_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
